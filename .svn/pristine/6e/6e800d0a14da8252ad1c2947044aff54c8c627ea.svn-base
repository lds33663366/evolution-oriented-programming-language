import java.util.regex.Matcher;
import java.util.regex.Pattern;

import structure.Property;

public class Test {

	public String obtainValue(String valueName) {

		String s1 = "hello";
		String s2 = s1;
		s2 = s2 + " world";
		System.out.println(s2);
		
	//	System.out.println("propName = " + propName);
		return "";

		// 如果
		// String str = "\\w+";
		// Pattern p = Pattern.compile(str);
		// Matcher m = p.matcher(valueName);
		//
		// int i = 1;
		// while (m.find()) {
		// String propName = m.group();
		// if (m.hitEnd()) {
		// System.out.println("第" + i + "个单词" + propName);
		// }
		// i++;
		// }
		//
		// return "";

	}

	public static void main(String[] args) {
		Test t = new Test();
		t.obtainValue("star");
	}

}
